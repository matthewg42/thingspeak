<h2 id="importer">Importer</h2>

<p>Using the ThingSpeak Importer, you are able to import data from a CSV file directly into a ThingSpeak Channel. The access the Importer, select a <em>Channel</em>, and click <em>Import Data</em>.</p>
<p>The format for the CSV should either of the following:</p>
<ol>
 <li>A CSV file with headers as generated by the export feature (<a title="ThingSpeak Import Sample Data" href="/files/importer_sample.csv" target="_blank">Sample CSV File</a>). Specifically the following columns are permissable in any order (provided the proper header rowis present):
  <ul>
    <li>datetime (server time will be used if not specified; time stamp can be in many formats such as epoch, ISO 8601, or MySQL time. If the datetime includes a GMT/UTC offset, we will us    e that to properly import the data. If your datetime stamps do not have a GMT / UTC offset, you can specify a time zone that the data was logged in.)</li>
    <li>entry_id (generated during export, discarded during import as a new ID will be generated for each imported record)</li>
    <li>latitude (optional)</li>
    <li>longitude (optional)</li>
    <li>elevation (optional)</li>
    <li>location (optional)</li>
    <li>status (optional)</li>
    <% Channel.column_names_with_prefix('field').each do |f| %>
      <li><%= f %> (optional)</li>
    <% end %>
   </li>
  </ul>
 <li>A headerless CSV file with the fields in the order above where all are mandatory (although the entry_id will still be discarded)
 </li>
</ol>
